<h1>登録記事一覧</h1>
<ul>

<% if !@books.nil? %>
<%  @books.each do |book| %>
 <li>
<%= book.article.title %>
<% if !book.article.ogpimg.nil? %>
  <%= image_tag(book.article.ogpimg) %>
  <% end %>
<br/>
<%= book.article.name %>
<%= book.article.date %><br/>
<%= book.article.category.description %>
<%= book.article.area.description %>
<% end %>
<% end %>
</li>
</ul>
